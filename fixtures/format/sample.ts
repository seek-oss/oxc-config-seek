const longCall = someFunction(argumentOne, argumentTwo, argumentThree, argumentNumberFour);
const obj = { alpha: 1, beta: 2, gamma: 3 };
const nested = {
  alpha: 1 };
const arrow = (x) => { return x + 1; };
const single = x => x;
const str = "double quotes here";
type Union = | 'a' | 'b' | 'c';
export function veryLongSignature(parameterOne: string, parameterTwo: number, three: boolean): void {}
const ternary = condition ? someRatherLongValueHere : anotherRatherLongValueThere;
class K { private readonly x = 1; method(): number { return this.x } }
const longArr = ['aaaaaaaaaa','bbbbbbbbbb','cccccccccc','dddddddddd','eeeeeeeeee','ffffffffff'];
if (a) { b() } else { c() }

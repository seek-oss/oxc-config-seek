//unspaced line comment
/*unspaced block comment*/
//*marker without a space

// A correctly spaced comment, which must NOT be reported.
/** A JSDoc block, which must NOT be reported. */
export const commented = 1;
